image: docker:20.10
services:
  - docker:dind       # To run all jobs in this pipeline, use a docker image which contains a docker daemon running inside (dind - docker in docker). Reference: https://forum.gitlab.com/t/why-services-docker-dind-is-needed-while-already-having-image-docker/43534

# ++++++++++++++++++++++++++++++++++++++++++ trufflehog-git-secrets +++++++++++++++++++++++++++++++++++++++++++++++

stages:
  - test
  - secret-detection
  - scanning

trufflehog-git-secrets:
  stage: test
  variables:
    outfile: "trufflehog-output.json"
    cert: "GIT_SSL_NO_VERIFY=true"
  script:
    - docker pull ghcr.io/trufflesecurity/trufflehog:latest
    # - docker run --user $(id -u):$(id -g) -v $(pwd):/src --rm hysnsec/trufflehog $CI_REPOSITORY_URL
    - echo $CI_REPOSITORY_URL
    - docker run --user $(id -u):$(id -g) -e $cert -v $(pwd):/src --rm ghcr.io/trufflesecurity/trufflehog:latest git $CI_REPOSITORY_URL --filter-entropy=3  --max-decode-depth=5 --fail --json | tee $outfile
   
  artifacts:
    paths:
      - $outfile
    untracked: false
    when: always
    access: all
    expire_in: 1 days
  allow_failure: true
  rules:
    - if: $CI_COMMIT_BRANCH 
  
# ++++++++++++++++++++++++++++++++++++++++++ Git Leaks +++++++++++++++++++++++++++++++++++++++++++++++

variables:
  # Setting this variable affects all Security templates
  # (SAST, Dependency Scanning, ...)
  SECURE_ANALYZERS_PREFIX: "$CI_TEMPLATE_REGISTRY_HOST/security-products"
  #
  SECRET_DETECTION_IMAGE_SUFFIX: ""
  SECRETS_ANALYZER_VERSION: "7"
  SECRET_DETECTION_EXCLUDED_PATHS: ""

.secret-analyzer:
  stage: test
  image: "$SECURE_ANALYZERS_PREFIX/secrets:$SECRETS_ANALYZER_VERSION$SECRET_DETECTION_IMAGE_SUFFIX"
  services: []
  allow_failure: true
  variables:
    GIT_DEPTH: "50"
  # `rules` must be overridden explicitly by each child job
  # see https://gitlab.com/gitlab-org/gitlab/-/issues/218444
  artifacts:
    access: 'all'
    reports:
      secret_detection: gl-secret-detection-report.json
    paths: [gl-secret-detection-report.json]

gitleaks_secret_detection:
  extends: .secret-analyzer
  rules:
    # The following 3 blocks of rules define whether the job runs in a an *MR pipeline* or a *branch pipeline*
    # when an MR exists. If the job has additional rules to observe they should be added in the blocks 1 and 3
    # to cover both the *MR pipeline* and the *branch pipeline* workflows.

    # 1. Run the job in an *MR* pipeline if MR pipelines for AST are enabled and there's an open merge request.
    - if: $AST_ENABLE_MR_PIPELINES == "true" &&
          $CI_PIPELINE_SOURCE == "merge_request_event"
    # 2. Don't run the job in a *branch pipeline* if *MR pipelines* for AST are enabled and there's an open merge request.
    - if: $AST_ENABLE_MR_PIPELINES == "true" &&
          $CI_OPEN_MERGE_REQUESTS
      when: never
    # 3. Finally, run the job in a *branch pipeline* (When MR pipelines are disabled for AST, or it is enabled but no open MRs exist for the branch).
    - if: $CI_COMMIT_BRANCH
  script:
    - /analyzer run




# ++++++++++++++++++++++++++++++++++++++++++ SMEGREP Tempaltes  +++++++++++++++++++++++++++++++++++++++++++++++

#Description: to run the Source code analyzer for all lanugage - in-built gitlab Tempaltes 

include:
    - template: Security/SAST.gitlab-ci.yml
    
# ++++++++++++++++++++++++++++++++++++++++++ Bandit - Python SOURCE CODE & Secrets ANALYZER +++++++++++++++++++++++++++++++++++++++++++++++

#Description: to run the pythion source code analyzer - bandit

Bandit-py-scans:
  stage: test
  image: python:3.6
  before_script:
   - pip3 install bandit
  script:
   - bandit -r . -f html | tee bandit-output.html
  artifacts:
    paths: [bandit-output.html]
    when: always
  allow_failure: true
  rules:
    - if: $CI_COMMIT_BRANCH

# ++++++++++++++++++++++++++++++++++++++++++ Brakeman - Puby & rails SOURCE CODE & Secrets ANALYZER +++++++++++++++++++++++++++++++++++++++++++++++

Brake-man-SAST:
  stage: test
  variables:
    ouutfile: "brakeman_results.html"
  image: 
    name: presidentbeef/brakeman:v8.0.5  
    entrypoint: ['']
  before_script:
    - rm -f $ouutfile    # for brakeman.igonre to exception
  script:
    - /usr/src/app/bin/brakeman -A -f html -o $ouutfile # cahnge html to json to get the json file
  artifacts:
    paths:
      - $ouutfile
    untracked: false
    when: always
    access: all
    expire_in: 1 days
  allow_failure: true
  rules:
    - if: $CI_COMMIT_BRANCH
  
  
 # ++++++++++++++++++++++++++++++++++++++++++ FindSecBugs java scan scan +++++++++++++++++++++++++++++++++++++++++++++++
 
 include:
  - template: Jobs/SAST.gitlab-ci.yml

variables:
  # Exclude Semgrep and enable SpotBugs (which includes FindSecBugs)
  #SAST_EXCLUDED_ANALYZERS: "semgrep, flawfinder, phpcs-security-audit, bandit, brakeman, gosec, mssqlsec, pmd-apex, security-code-scan, sobelow, webgoat"
  SAST_EXCLUDED_ANALYZERS: "semgrep"

spotbugs-sast:
  extends: .sast-analyzer
  variables:
    SAST_JAVA_VERSION: "17" # Adjust to match your project's Java version (e.g., 11, 17, 21)
  after_script:
   # - echo "Current directory:" $(pwd)
   # - find . -name "*.json"
   # - find /tmp -name "*.json" 2>/dev/null
   # - find / -name "gl-sast-report.json" 2>/dev/null
  rules:
    - if: $CI_COMMIT_BRANCH # Runs the scan on every commit
  artifacts:
    name: sast
    paths:
      - gl-sast-report.json
    reports:
      sast: gl-sast-report.json
    when: always
